<h1>Eat Da Burger</h1>

<h2>Add a New Burger</h2>

<div class="brgrBtn" id="newBrgr">
  <label for="brgr">Burger Name</label>
  <input type="text" id="brgr" name="burger">
  <button type="submit" id="brgrSbmt">Add Burger</button>
</div>

<h2>Choose a burger then devour it!</h2>

<h3>Burgers Eaten</h3>

<ul>
  {{#each burger}}
  {{#if devoured}}
  <li>{{burger_name}}</li>
  <li>Devoured? {{devoured}}</li>
  {{/if}}
  {{/each}}
</ul>

<h3>Burgers To Eat</h3>

<ul>
  {{#each burger}}
  {{#unless devoured}}
  <li id="toEat">{{burger_name}}</li>
  <li>Devoured? {{devoured}}</li>
  {{/unless}}
  {{/each}}
</ul>

{{!-- <h2>Add a Cat</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Cat Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="sleepy">Sleepy?</label><br>
    <input type="radio" name="sleepy" value="1" checked> Sleepy!<br>
    <input type="radio" name="sleepy" value="0"> Awake
  </div>

  <button type="submit">Add Cat</button>
</form> --}}